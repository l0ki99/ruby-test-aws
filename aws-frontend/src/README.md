# README

You should be able to run this from the parent directory with:

`docker-compose build`

and

`docker-compose up`

If you want to run this folder manually, install NodeJS.

then from this folder:

`npm install`

`npm run dev`
