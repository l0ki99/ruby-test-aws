# README

You should be able to run `docker-compose build` and `docker-compose up` in the parent directory to get up and running.

If you want to run to this manually you should be able to:

1. Ensure Ruby and bundler are installed.
1. `bundle install` from this directory
1. `rails db:migrate db:seed`
1. `rails s`
